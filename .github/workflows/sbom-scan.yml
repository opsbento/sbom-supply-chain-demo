name: sbom-scan

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  generate-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Syft
        id: syft
        uses: anchore/sbom-action/download-syft@v0

      - name: Generate CycloneDX SBOM from app folder
        run: ${{ steps.syft.outputs.cmd }} dir:app -o cyclonedx-json=sbom.cdx.json

      - name: Install Grype
        id: grype
        uses: anchore/scan-action/download-grype@v6

      - name: Scan SBOM with Grype
        run: ${{ steps.grype.outputs.cmd }} sbom:sbom.cdx.json --fail-on high
